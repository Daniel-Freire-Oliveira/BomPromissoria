<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Promissoria - login</title>
    <link rel="stylesheet" href="/bootstrap.min.css" />

    <style>
        body {
            background-image: url('background-image.jpg'); /* Substitua 'background-image.jpg' pela URL da sua imagem de fundo */
            background-size: cover;
            background-position: center;
        }
        .card {
            margin-top: 5%;
        }
        .step-indicator {
            text-align: center;
            margin-bottom: 20px;
        }
        .step-indicator .step {
            display: inline-block;
            width: 30px;
            height: 30px;
            line-height: 30px;
            background-color: #6c757d;
            color: white;
            border-radius: 50%;
            margin: 0 5px;
        }
        .step-indicator .active {
            background-color: #0d6efd;
        }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center">
        <div class="card p-4 shadow-lg">
            <div class="card-body">
                <h3 class="card-title text-center">Login</h3>
                <form action="/login" method="post" >
                    <div class="mb-3">
                        <label for="firstName" class="form-label">Usuário</label>
                        <input type="text" name="nm" class="form-control" id="firstName" placeholder="Ex:Fulano">
                    </div>
                    <div class="mb-3">
                        <label for="password"  class="form-label">Senha</label>
                        <input type="password" name="psw" class="form-control" id="password" placeholder="Ex: 123456.">
                    </div>
                    <div class="d-grid">
                        <button type="submit" id="entrar" class="btn btn-primary">Entrar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    </body>
</html>
